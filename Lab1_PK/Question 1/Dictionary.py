# Creating an empty dictionary
myDict = {}
noStudents = input('No. of students: ')
for i in range noStudents
myDict[] = input('Enter the student name: ')
myDict[i] = input('Enter the subject enrolled in: ')
myDict[i] = input('Enter the score obtained: ')
print(myDict)
